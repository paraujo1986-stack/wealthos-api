[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma generate && npx prisma migrate deploy && node src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
